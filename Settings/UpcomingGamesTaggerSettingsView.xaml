<UserControl x:Class="UpcomingGamesTagger.UpcomingGamesTaggerSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <StackPanel Margin="20">
        <TextBlock Text="{DynamicResource LOCUpcomingGamesTaggerSettingsTagSettings}" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>

        <TextBlock Text="{DynamicResource LOCUpcomingGamesTaggerSettingsTagName}" Margin="0,5,0,5"/>
        <TextBox Text="{Binding Settings.TagName}" Margin="0,0,0,10"/>

        <CheckBox IsChecked="{Binding Settings.AutoUpdateOnLibraryChange}"
                  Content="{DynamicResource LOCUpcomingGamesTaggerSettingsAutoUpdate}" Margin="0,5"/>

        <CheckBox IsChecked="{Binding Settings.ShowNotifications}"
                  Content="{DynamicResource LOCUpcomingGamesTaggerSettingsShowNotifications}" Margin="0,5"/>

        <TextBlock Text="{DynamicResource LOCUpcomingGamesTaggerSettingsGameSelection}" FontWeight="Bold" FontSize="14" Margin="0,15,0,10"/>

        <CheckBox IsChecked="{Binding Settings.IncludeGamesWithoutReleaseDate}"
                  Content="{DynamicResource LOCUpcomingGamesTaggerSettingsIncludeNoDate}" Margin="0,5"/>

        <TextBlock Text="{DynamicResource LOCUpcomingGamesTaggerSettingsDaysThreshold}" Margin="0,10,0,5"/>
        <TextBox Text="{Binding Settings.DaysAheadThreshold}" Margin="0,0,0,5"/>
        <TextBlock Text="{DynamicResource LOCUpcomingGamesTaggerSettingsDaysThresholdHint}"
                   FontStyle="Italic" FontSize="10" Foreground="Gray" Margin="0,0,0,10"/>
    </StackPanel>
</UserControl>