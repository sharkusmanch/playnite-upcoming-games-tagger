<UserControl x:Class="UpcomingGamesTagger.UpcomingGamesTaggerSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <StackPanel Margin="20">
        <TextBlock Text="Tag Settings" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>
        
        <TextBlock Text="Tag Name:" Margin="0,5,0,5"/>
        <TextBox Text="{Binding Settings.TagName}" Margin="0,0,0,10"/>
        
        <CheckBox IsChecked="{Binding Settings.AutoUpdateOnLibraryChange}" 
                  Content="Automatically update tag when library changes" Margin="0,5"/>
        
        <CheckBox IsChecked="{Binding Settings.ShowNotifications}" 
                  Content="Show notifications when tag is updated" Margin="0,5"/>
        
        <TextBlock Text="Game Selection" FontWeight="Bold" FontSize="14" Margin="0,15,0,10"/>
        
        <CheckBox IsChecked="{Binding Settings.IncludeGamesWithoutReleaseDate}" 
                  Content="Include games without release dates" Margin="0,5"/>
        
        <TextBlock Text="Days ahead threshold (0 = no limit):" Margin="0,10,0,5"/>
        <TextBox Text="{Binding Settings.DaysAheadThreshold}" Margin="0,0,0,5"/>
        <TextBlock Text="Only include games releasing within this many days" 
                   FontStyle="Italic" FontSize="10" Foreground="Gray" Margin="0,0,0,10"/>
    </StackPanel>
</UserControl>